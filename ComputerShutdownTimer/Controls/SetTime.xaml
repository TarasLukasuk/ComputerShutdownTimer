<UserControl x:Class="ComputerShutdownTimer.Controls.SetTime"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="RoundButton" TargetType="Button">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#9B59B6"/>
            <Setter Property="BorderBrush" Value="#8E44AD"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="5"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="100" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#8E44AD"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#7D3C98"/>
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black" Opacity="0.6" BlurRadius="10"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#7D3C98"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#6C3483"/>
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black" Opacity="0.8" BlurRadius="3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel Orientation="Vertical">
            <Label Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" 
                   Content="{Binding LableText, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

            <Border Background="#2C3E50" MinWidth="240" MinHeight="70" CornerRadius="35" Padding="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="Reduce" Style="{StaticResource RoundButton}" Content="&#60;" Grid.Column="0" Click="Reduce_Click"/>

                    <Border CornerRadius="100" Background="#9B59B6" Grid.Column="1" Width="70" Height="50">
                        <Label x:Name="Count" Content="00" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" 
                               FontSize="20" FontWeight="Bold"/>
                    </Border>

                    <Button x:Name="Increase" Style="{StaticResource RoundButton}" Content="&#62;" Grid.Column="2" Click="Increase_Click"/>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

<Window x:Name="window" x:Class="ComputerShutdownTimer.Views.Forms.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComputerShutdownTimer.Views.Forms"
        mc:Ignorable="d"
        Title="Computer shutdown timer"
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize">

    <Grid>
        <Border Background="#1B1A4A" CornerRadius="20"/>

        <Border Background="#26255B" Height="45" CornerRadius="20,20,0,0" VerticalAlignment="Top">
            <Grid Margin="10">
                <Image Width="30" HorizontalAlignment="Left"/>
                <TextBlock Text="{Binding Title, ElementName=window}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#F0F0F0" FontWeight="Bold" FontSize="14"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <CheckBox/>
                    <Button/>
                    <Button/>
                    <Button/>
                    <Button/>
                </StackPanel>
            </Grid>
        </Border>

        <Rectangle VerticalAlignment="Top" Height="10" Fill="Transparent" Cursor="SizeNS" Margin="10,0"/>
        <Rectangle VerticalAlignment="Bottom" Height="10" Fill="Transparent" Cursor="SizeNS" Margin="10,0"/>
        <Rectangle HorizontalAlignment="Left" Width="10" Fill="Transparent" Cursor="SizeWE" Margin="0,10"/>
        <Rectangle HorizontalAlignment="Right" Width="10" Fill="Transparent" Cursor="SizeWE" Margin="0,10"/>
        <Rectangle HorizontalAlignment="Left" VerticalAlignment="Top" Width="10" Height="10" Fill="Transparent" Cursor="SizeNWSE"/>
        <Rectangle HorizontalAlignment="Right" VerticalAlignment="Top" Width="10" Height="10" Fill="Transparent" Cursor="SizeNESW"/>
        <Rectangle HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="10" Height="10" Fill="Transparent" Cursor="SizeNESW"/>
        <Rectangle HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="10" Height="10" Fill="Transparent" Cursor="SizeNWSE"/>

        <Grid Margin="20,60,20,20">
            
        </Grid>
    </Grid>
</Window>
